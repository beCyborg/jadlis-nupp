# Changelog — nupp

Формат: [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/), версии — [SemVer](https://semver.org/lang/ru/).
До 1.0.0 совет жил в плагине `advisors` 1.0.0–1.0.3 (репо `advisors`, теперь приватный источник `jadlis-advisors-source`).

## [Unreleased]

## [1.0.0] — 2026-09-07

### Для человека

- Первый релиз под именем `nupp`: выделен из `advisors` 1.0.3 — репо на совет, команда `/nupp`, ключ настройки `MEMORY_DIR`.

### For agents

- Generated by `tools/split-councils.py` of `jadlis-advisors-source`; worker agent `nupp:advisor-opus`; `SHARED_FROM.txt` carries the source tag + content hash checked by CI.
